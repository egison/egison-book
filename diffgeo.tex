\chapter{Egisonで計算する微分幾何}\label{diffgeo}

本章は，数式処理システムとしてのEgisonの実践的な応用例として微分幾何への応用を紹介する．
Egisonを使うと，さまざまな多様体の曲率が定義の数式に近いプログラムを記述するだけで計算できたり，外微分やホッジ作用素のような微分形式のための作用素もプログラムで簡潔に定義できる．
本章の解説は読者に微分幾何の知識を仮定している．
微分幾何に興味を持つ読者には，小林昭七による『曲線と曲面の微分幾何』（裳華房）がおすすめである．

\section{微分幾何とEgison}

微分幾何は手計算がとくに大変な分野である．
例えば，曲率が自明ではないもっとも単純な多様体は球面$S^2$であるが，そのリーマン曲率テンソルを手計算すると，初めての場合，30分くらいかかる．
続けて，その次に単純な多様体であるトーラス$T^2$のリーマン曲率テンソルを計算すると，またしても30分以上はかかる．
実際の研究のためには，より高次元の複雑な多様体の曲率を計算することが多い．
このような場合，計算に一ヶ月以上かかることもある．
そのため，微分幾何は数式処理システムがもっとも応用しやすい分野でもある．

数式処理システムを使えば，$S^2$のリーマン曲率テンソルは，球面座標系，リーマン計量，クリストッフェル記号，リーマン曲率テンソルの定義をプログラムとして記述すれば，数秒で計算できる．
$T^2$については，$S^2$のリーマン曲率テンソルを計算をするプログラムを書いた後であれば，座標系の設定以外の部分は使いまわせるため，数分の作業で計算できる．
より高次元の複雑な多様体についても，計量の設定を変更するだけで，計算することができる．

Egisonは，\ref{cas-derivative}節で解説した微分演算子，第\ref{tensor}章で解説したテンソルのための言語機能，第\ref{funcsym}章で解説した関数シンボルを組み合わせて，従来の数式処理システムよりも数学の記法に近い形で微分幾何の計算をプログラムとして記述できる．
また，\ref{tensor-completion}節で触れたように，外微分やホッジ作用素のような微分形式のための作用素もプログラムで簡潔に定義するための仕組みも提供している．
本章では，これらの機能を実演し，微分幾何の研究・教育にEgisonが役に立つことを紹介する．

\section{リーマン曲率テンソルの計算}\label{diffgeo-riemann}

% 流れ:
% 微分幾何においてもっとも重要な概念であるリーマン曲率テンソルを計算してみる．
% あらゆる微分幾何の計算において曲率の計算は前提にある．(不変量は曲率から計算される．)
% 基本的に計量の部分だけを変えれば，あらゆる多様体の曲率を計算できる．
% 本節を読めば，実際の微分幾何の研究にもEgisonが使えるはずである．

本節は，球面について，リーマン計量や，クリストッフェル記号，リーマン曲率テンソル，リッチ曲率，スカラー曲率といった様々な微分幾何的な量をEgisonを使って計算するプログラムを解説する．
図\ref{fig:riemann-curavture-of-S2}は，Jupyter Notebook上で球面のリーマン曲率テンソルを計算するEgisonプログラムとその実行結果の出力である．
さまざまな幾何学的不変量は，曲率から計算される．
本節で解説するプログラムの計量の部分だけを変えれば，あらゆる多様体の曲率を計算できる．
そのため，本節のプログラムを理解すれば，実際の微分幾何の研究にもEgisonが使える．
本節は，このプログラムの各箇所について順番に説明していくことによって進める．

\begin{figure}[t]
  \centering
  \includegraphics[width=14.0cm,bb=0 0 1791 2069]{riemann-curvature-S2.png}
  \caption{球面のリーマン曲率テンソルの計算}
  \label{fig:riemann-curavture-of-S2}
\end{figure}

\subsection{球座標系 - \texttt{[1]}-\texttt{[2]}}

球座標系は，$r$と$θ$，$φ$の3つのパラメーターからなる．
$r$は原点からの距離をあらわす．
$θ$は$z$軸との角度，緯度をあらわす．
$φ$は$xy$平面における$x$軸からの回転量，経度をあらわす．
球座標系の$r$の値を固定すれば，球面の座標系を得ることができる．
球座標系とデカルト座標系は，$x = r \cos \theta \sin \phi, y = r \cos \theta \cos \phi, z = r \sin \theta$という関係式で結ばれている．
以上が，図\ref{fig:riemann-curavture-of-S2}の\texttt{[1]}と\texttt{[2]}で表現されている内容である．

\subsection{接ベクトルとリーマン計量 - \texttt{[3]}-\texttt{[7]}}

球面上の$θ$，$φ$によって定まる各点の上の接空間の基底となるベクトルをまず計算している．
\texttt{[3]}でそのようなベクトルを計算し，\texttt{[4]}でそのベクトルを表示している．
\texttt{[4]}の出力の$1$行目と$2$行目が基底となるベクトルの成分である．

リーマン計量は，各点における接ベクトルの大きさや向きの情報をもつ行列として表現できる．
\texttt{[5]}でベクトルの内積を計算すことによって基底ベクトルの大きさが計算されている．
\texttt{[6]}では，\texttt{[5]}で定義したリーマン計量の値を表示している．
球座標系においては，座標から自然に定まる基底の大きさが，各点で異なる．
たとえば，経度（$\phi$）方向の基底ベクトルの大きさが，緯度（$\theta$）によって変わる．
この情報は，\lstinline|g_i_j|の$2,2$成分の$r^2 \sin^2 \theta$にあらわれている．

\texttt{[7]}では，逆行列を計算する関数\lstinline{M.inverse}を使って，上添字がつくリーマン計量を求めている．
リーマン計量には下添字がつく$g_{ij}$と上添字がつく$g^{ij}$の2種類がある．

\subsection{クリストッフェル記号とリーマン曲率テンソル - \texttt{[8]}-\texttt{[12]}}

%TODO: クリストッフェル記号の簡単な説明．

%クリストッフェル記号は，直感的には，基底のねじれ具合を表現している．
%ある基底ベクトルが作るベクトル場に沿ってベクトルを動かすとそのベクトルの向きや成分表示したときの値の大きさが変わることがある．
%このベクトルの変化の具合がクリストッフェル記号のそれぞれの成分によって表現される．

\texttt{[8]}と\texttt{[9]}は，第一種クリストッフェル記号と第二種クリストッフェル記号の数式による定義をプログラムとして記述したものである．
数式に非常に近いかたちでプログラムとして表現されている．
直感的には，クリストッフェル記号$\Gamma^{i}_{\;jk}$は，基底ベクトル$e_j$を$e_k$方向に動かしたときの$e_i$方向への変化の具合を表現する．

\begin{alignat*}{2}
\Gamma_{ijk} = \frac{1}{2} (\frac{\partial g_{ij}}{\partial x^k} + \frac{\partial g_{ik}}{\partial x^j} - \frac{\partial g_{kj}}{\partial x^i}) &\qquad
\Gamma^{i}_{\;jk} = g^{im} \Gamma_{mjk} &\qquad
\end{alignat*}

リーマン曲率テンソルも，\texttt{[11]}のように，その数式による定義をそのままプログラムとして記述できている．
直感的には，リーマン曲率テンソル$R^{i}_{\;jkl}$は，$e_k$と$e_l$によって作られる閉路に沿って$e_j$を動かしたときの$e_i$方向への変化の具合を表現する．

\[R^{i}_{\;jkl} = \frac{\partial \Gamma^{i}_{\;jl}}{\partial x^k} - \frac{\partial \Gamma^{i}_{\;jk}}{\partial x^l} + \Gamma^{m}_{\;jl} \Gamma^{i}_{\;mk} - \Gamma^{m}_{\;jk} \Gamma^{i}_{\;ml} \]

\subsection{リッチ曲率とスカラー曲率 - \texttt{[13]}-\texttt{[16]}}

\todo{}

\subsection{計量を変えてみる}

本節で解説したプログラムの計量の部分を変えるだけで，いろいろな多様体のクリストッフェル記号やリーマン曲率テンソルを計算できる．

たとえば，トーラス$T^2$のリーマン曲率テンソルを計算するには，球座標系をトーラスの座標系に変更すればよい．
トーラスの座標系はたとえば，図\ref{fig:riemann-curvature-T2}の\texttt{1}-\texttt{2}のように設定できる．
バッククォート(``\lstinline|`|'')が，この式の中で使われている．
\ref{mexpr-backquote}節で解説したように，バッククォートのついた数式は，Egison処理系に一つのシンボルと同じように扱われる．
ここでは，バッククォートを適切に挿入することより，計算プロセスや，最終的な計算結果が単純になっている．

\begin{figure}[t]
  \centering
  \includegraphics[width=13.5cm,bb=0 0 1784 2133]{riemann-curvature-T2.png}
  \caption{トーラスのリーマン曲率テンソルの計算}
  \label{fig:riemann-curavture-of-S2}
\end{figure}

また，計量を以下のように定義されるSchwarzschild計量に変えれば，原点を中心に一つだけ質量をもつ天体がある宇宙についての一般相対性理論の計算ができる．
この計算結果を解釈すれば，「時空の曲がり=重力」ということが理解できる．\cite{fuji1979}

\begin{lstlisting}[language=egison,numbers=none,escapechar=']
def g_i_j := [| [| `(c^2 * r - 2 * G * M) / c^2 * r, 0, 0, 0 |]
                [| 0, -1 * c^2 * r / `(c^2 * r - 2 * G * M), 0, 0 |]
                [| 0, 0, -1 * r^2, 0 |]
                [| 0, 0, 0, -1 * r^2 * (sin `$\theta$`)^2 |] |]
\end{lstlisting}

\section{曲率形式の計算 - 微分形式を使った計算（1）}\label{diffgeo-curvature}

\ref{tensor-tensorfunc}節や，\ref{tensor-completion}節，\ref{tensor-refs}節で言及したように，Egisonのテンソル関連の機能は微分形式を使った計算を簡潔に表現できるように設計されている．
微分形式を使うと，テンソルに関する演算をより抽象的に記述できるようになる．

本節では，微分形式の計算の例として，曲率形式の計算を紹介する．
曲率形式は，リーマン曲率テンソルを一般化したもので，曲率形式の公式$\Omega^{i}_{\;j} = d \omega^{i}_{\;j} + \omega^{i}_{\;k} \wedge \omega^{k}_{\;j}$というリーマン曲率テンソルの公式よりも簡潔な形の式を使って計算できる．
上記の公式にあらわれる$\omega$は接続形式と呼ばれ，微分形式において第二種クリストッフェル記号に対応するものである．

\begin{figure}[t]
  \centering
  \includegraphics[width=14.0cm,bb=0 0 1787 1314]{curvature-form.png}
  \caption{曲率形式の計算}
  \label{fig:curvature-form}
\end{figure}

\subsection{ウェッジ積・曲率形式}

図\ref{fig:curvature-form}は$S^2$の曲率形式をEgisonで計算したプログラムである．
\texttt{[1]}-\texttt{[5]}で球面座標系のパラメータと，リーマン計量，クリストッフェル記号を設定している．
\texttt{[6]}で接続形式$\omega$を第二種クリストッフェル記号を使って定義している．
\texttt{[7]}で外微分演算子を定義している．
\texttt{[8]}-\texttt{[9]}でウェッジ積を定義している．
ウェッジ積は中置演算子として定義されている．
\texttt{[10]}-\texttt{[12]}で曲率形式を計算して出力している．
\todo{}

\subsection{オイラー形式とオイラー数}

オイラー類は，積分したらオイラー数が得られる特性類として有名な特性類である．
以下のプログラムは，トーラス$T^2$のオイラー形式を計算している．

\begin{lstlisting}[language=egison,numbers=none]
def eulerForm := (1 / (2 * `$\pi$`)) * (`$\Omega$`~1_2 - `$\Omega$`~2_1)

eulerForm -- [| [| 0, (cos `$\theta$`) / (* 2 `$\pi$`)) |], [| ((-1 * (cos `$\theta$`)) / (* 2 `$\pi$`)), 0 |] |]
\end{lstlisting}

積分のための関数がEgisonには実装されていないため，手計算でこれを積分した結果を記す．
積分すると$T^2$のオイラー数である$0$が得られる．

\[\chi(T^2) = \int_{T^2} d \theta d \phi \frac{\cos \theta}{2 \pi} = \int_{0}^{2 \pi} d \theta \cos \theta = \left[\sin \theta \right]_{0}^{2 \pi} = \sin 2 \pi - \sin 0 = 0\]

\section{ホッジ・ラプラシアンの計算 - 微分形式を使った計算（2）}\label{diffgeo-hodge}


本節では，微分形式を使った計算をプログラムする例を紹介する．

図\ref{fig:hodge-laplacian}は，極座標のラプラシアンをホッジ・ラプラシアンの公式$\Delta = d \delta + \delta d$を使って計算するEgisonプログラムとその実行結果である．
本節の前半は，このプログラムの解説をしながら進む．

\begin{figure}[t]
  \centering
  \includegraphics[width=14.0cm,bb=0 0 1759 1341]{laplacian.png}
  \caption{極座標のホッジ・ラプラシアンの計算}
  \label{fig:hodge-laplacian}
\end{figure}

\subsection{外微分・ホッジ作用素}

図\ref{fig:hodge-laplacian}の前半\texttt{[1]}-\texttt{[4]}は，極座標系のパラメーターと計量を設定している．
その後，外微分・ホッジ作用素・外微分の随伴作用素という微分形式の基本的な演算子を\texttt{[5]}-\texttt{[8]}で定義している．
\texttt{[6]}のホッジ作用素の定義には以下の公式を使っている．
この定義のなかで使われている\texttt{subrefs}は，第二引数のシンボルのリストを，第一引数のテンソルの下添字として順番に付加する組み込み関数である．

\[*A = \sqrt{\det|g|} \cdot \epsilon_{i_{1}...i_{n}} \cdot A_{j_{1}...j_{k}} \cdot g^{i_{1}j_{1}} \cdots g^{i_{k}j_{k}} \cdot e^{i_{k+1}} \wedge ... \wedge e^{i_{n}} \]

\subsection{ホッジ・ラプラシアン}

微分形式を使うと，ラプラシアンは$\Delta = d \delta + \delta d$と座標系に依存しないように定義できる．
この公式を使って表現されるラプラシアンはホッジ・ラプラシアンと呼ばれる．
\texttt{[8]}においてこの公式がEgisonで表現されている．
\texttt{[8]}の定義において，$0$形式と$2$形式の場合が特別扱いされている．
これは，$2$次元座標系における$2$形式にたいして$d$が，$0$形式にたいして$\delta$が，厳密には未定義であるためである．

\texttt{[9]}で定義した関数シンボルに，\texttt{[10]}でホッジ・ラプラシアンを適用している．
\ref{riemann}節で解説したリーマン曲率テンソルと同様，ホッジ・ラプラシアンについても計量を変えるだけで，いろいろな座標系のラプラシアンを計算できる．

\section{その他の微分形式についての演算子 - 内部積・リー微分}

内部積やリー微分も，Egisonで簡潔に定義できることを紹介しておく．
これらの演算子は，たとえば，流体力学の質量保存の法則やナビエ・ストークス方程式の微分形式による表現をするときに使う．
内部積は以下のように定義できる．
\begin{lstlisting}[language=egison,numbers=none]
def `$\iota$` %X %Y :=
  withSymbols [i] dfOrder Y * X...~i . (antisymmetrize Y..._i)
\end{lstlisting}
リー微分は以下のように定義できる．
\begin{lstlisting}[language=egison,numbers=none]
Lie %X %Y :=
  match dfOrder Y as integer with
    | #0 -> `$\iota$` X (d Y)
    | #N -> d (`$\iota$` X Y)
    | _  -> `$\iota$` X (d Y) + d (`$\iota$` X Y)
\end{lstlisting}


